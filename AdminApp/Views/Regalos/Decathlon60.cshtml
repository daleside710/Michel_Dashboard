@{
    ViewData["Title"] = "TARJETA REGALO DECATHLON 60€ | Detalle Regalo | Vuelta al Cole | Michelin";
    var currentUrl = Context.Request.Path;
    var seleccionBtn = false;
    if (currentUrl.ToString().Contains("-seleccion"))
    {
        seleccionBtn = true;
    }
    var hiddenidsession = ViewBag.hiddenidsession;
}
@section Styles{
<link rel="stylesheet" href="~/css/promomichelin.css" />
    <style>
      
        #containerRegalo {
            max-width: 750px;
        }

    </style>
}
<div class="content contentMichelinRegalo">
    <div class="container containerMichelinRegalo">
        <div id="containerRegalo">
            <div class="volver">
                @if (seleccionBtn)
                {
                    <a href="@Url.Action("Regalo", "Seleccioneumatico")">< Volver</a>
                }
            </div>
            <div class="row" style="margin: 30px 0px 30px 0px;">
                <div class="col-xl-5 col-lg-5 col-md-5 col-sm-12 col-xs-12 leftPremio">
                    <img src="~/images/premios/es/regalo_decathlon.png" alt="TARJETA REGALO DECATHLON" class="img-fluid" style="max-width:300px;" />
                </div>
                <div class="col-xl-7 col-lg-7 col-md-7 col-sm-12 col-xs-12">
                    <div class="titPremio">TARJETA REGALO DECATHLON 60€</div>
                    <div class="txtPremio">
                        <p>Prepárate para la vuelta al cole. Canjea tu Cheque Regalo de Decathlon por el producto que prefieras en cualquier tienda Decathlon de España, ¡hay más de 150! O si prefieres puedes canjearlo en www.decathlon.es</p>
                        <p>Se puede utilizar el saldo del cheque y realizar compras hasta agotar la totalidad del saldo.</p>
                        <p>El pago con el cheque puede efectuarse en una o varias veces hasta agotar el saldo del mismo.</p>
                    </div>
                    @if (seleccionBtn)
                    {
                        <form id="regalo-detail-form" action="seleccion-regalo-detail-form" method="post">
                            <input type="hidden" name="hiddenidsession" value="@hiddenidsession">
                            <div class="frmBtn"><div class="btnPremio">Seleccionar</div></div>
                        </form>
                    }
                    else
                    {
                        <div class="frmBtn"><div class="btnPremio Volver">Volver</div></div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script>
        $("body").addClass("bindex_det");
        $(".btnPremio").on('click', function () {
            var form = $("form[id=regalo-detail-form]");
            form.submit();
        });
        $(".Volver").on('click', function () {
            window.location.href = "@Url.Action("Index", "Regalos")";
        });
    </script>
}